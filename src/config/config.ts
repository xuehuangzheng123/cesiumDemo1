// ? 全局不动配置项 只做导出不做修改

// * 首页地址（默认）
export const HOME_URL: string = "/overview/index";

// * 默认主题颜色
export const DEFAULT_PRIMARY: string = "#009688";

// * Tabs（白名单地址，不需要添加到 tabs 的路由地址）
export const TABS_WHITE_LIST: string[] = ["/403", "/404", "/500"];

// * client_id（默认）
export const CLIENT_ID: string = "tudou_sa";
